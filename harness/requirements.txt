# Microsoft Agent Framework (umbrella package). Pulls in agent-framework-core
# plus the provider integrations, including agent_framework.foundry
# (FoundryChatClient) used to talk to the Azure AI Foundry project.
agent-framework==1.9.0

# Shell tool used by the harness (LocalShellTool). Separate package; provides
# agent_framework_tools.shell. Required for create_harness_agent(shell_executor=...).
agent-framework-tools==1.0.0a260424

# Textual-based console UI. The harness drives the upstream `console` package
# from the microsoft/agent-framework harness sample (referenced via the sparse
# git submodule under external/agent-framework, not vendored). textual + rich are
# that package's runtime dependencies. Run scripts/setup-console.sh once to fetch it.
textual>=6.2.1
rich>=13.7.1

# Azure auth (AzureCliCredential locally; DefaultAzureCredential in other envs).
azure-identity>=1.15.0

# Loads FOUNDRY_PROJECT_ENDPOINT / FOUNDRY_MODEL from a local .env file.
python-dotenv>=1.0.0

# Used by the bundled `pptx` skill (skills/pptx) to build PowerPoint decks from a
# JSON slide description. The skill's script is run by the agent via the shell tool.
python-pptx>=1.0.0
